<div class="small-menu collapse navbar-collapse" id="bs-example-navbar-collapse-1">
  <ul class="nav navbar-nav active">
    <li><%= link_to "My Blogs", blogs_path(user_id: current_user.id), id: "menu-font"   if current_user != nil%></li>
    <li><%= link_to "My Profile", user_path(current_user), id: "menu-font"   if current_user != nil%></li>
    <li><%= link_to "Create New Blog", new_blog_path, id: "menu-font" %></li>
    <li><%= link_to "Create New Post", new_post_path, id: "menu-font" %></li>
    <% if can? :manage, :all %>
      <li><%= link_to "Admin Panel", admin_path, id: "menu-font" %></li>
    <% end %>
  </ul>
    <ul class="nav navbar-nav navbar-right">
      <% if user_signed_in? %>
        <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete, id: "menu-font-sign" %></li>
      <% else %> 
         <li><%= link_to "Sign In", new_user_session_path, :method => :get, id: "menu-font-sign"  %></li>
         <li><%= link_to "Sign Up", new_user_registration_path, :method => :get, id: "menu-font-sign"  %></li>
      <% end %>
    </ul>
</div>

